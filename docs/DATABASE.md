# Database Schema

> PostgreSQL database design for VelaWear commercial e-commerce backend.
> Update this file whenever schema changes.

---

## Engine & Naming Conventions

| Item | Convention |
|------|------------|
| Database | PostgreSQL |
| Table names | `snake_case`, plural for main entities, singular join table names kept as project convention |
| Primary keys | `BIGINT GENERATED BY DEFAULT AS IDENTITY` unless stated otherwise |
| Money | `NUMERIC(15,2)` |
| Date/time | `TIMESTAMPTZ` for audit and event timestamps, `DATE` for birth date |
| Soft delete | `deleted_at TIMESTAMPTZ NULL`, indexed when present |
| Enum-like values | `VARCHAR`, mapped by JPA with `@Enumerated(EnumType.STRING)` |

Notes:
- PostgreSQL does not support an automatic `updated_at` column clause directly. Use a database trigger such as `set_updated_at()` for tables with `updated_at`.
- `deleted_at` means soft delete. Every table with a `deleted_at` index must have a cleanup/anonymization job policy.
- All foreign keys should be explicitly named in Flyway migrations for easier maintenance.

---

## Entity Relationship Diagram

Full ERD for the current 28-table VelaWear schema. Main business relationships use direct `N:M` notation for readability; physical join tables are listed separately.

```text
RBAC / Account
┌──────────────────┐       N:M       ┌──────────────┐       N:M       ┌──────────────────┐
│      users       │◄───────────────►│    roles     │◄───────────────►│   permissions    │
├──────────────────┤                 ├──────────────┤                 ├──────────────────┤
│ id (PK)          │                 │ id (PK)      │                 │ id (PK)          │
│ full_name        │                 │ name (UQ)    │                 │ name             │
│ email (UQ)       │                 │ description  │                 │ api_path         │
│ password         │                 │ created_at   │                 │ method           │
│ birth_date       │                 │ updated_at   │                 │ module           │
│ avatar           │                 └──────────────┘                 │ created_at       │
│ gender           │                                                  │ updated_at       │
│ created_at       │                                                  └──────────────────┘
│ updated_at       │
│ deleted_at       │
└──────┬───────────┘
       │ 1:N
       ├──────────────────► refresh_tokens(id, token, user_id, expires_at, revoked, device_info, ip_address, created_at)
       │ 1:N
       ├──────────────────► user_addresses(id, user_id, receiver_name, phone, province, district, ward, address_detail, is_default)
       │ 1:1
       └──────────────────► carts(id, user_id, created_at)

Physical join tables:

┌──────────────────┐       ┌───────────────────┐
│    user_role     │       │  permission_role  │
├──────────────────┤       ├───────────────────┤
│ user_id (PK/FK)  │       │ permission_id     │
│ role_id (PK/FK)  │       │ (PK/FK)           │
└──────────────────┘       │ role_id (PK/FK)   │
                           └───────────────────┘

Catalog
┌──────────────┐       1:N       ┌──────────────────┐       N:1       ┌──────────────────┐
│    brands    │◄───────────────│    products      │───────────────►│   categories     │
├──────────────┤                 ├──────────────────┤                 ├──────────────────┤
│ id (PK)      │                 │ id (PK)          │                 │ id (PK)          │
│ name         │                 │ name             │                 │ name             │
│ slug (UQ)    │                 │ slug (UQ)        │                 │ slug (UQ)        │
│ description  │                 │ description      │                 │ parent_id (FK)   │
│ status       │                 │ category_id (FK) │                 │ status           │
│ created_at   │                 │ brand_id (FK)    │                 │ sort_order       │
│ updated_at   │                 │ status           │                 │ created_at       │
│ deleted_at   │                 │ created_at       │                 │ updated_at       │
└──────────────┘                 │ updated_at       │                 │ deleted_at       │
                                 │ deleted_at       │                 └────────┬─────────┘
                                 └──────┬───────────┘                          │ 1:N self-reference
                                        │ 1:N                                  ▼
                    ┌───────────────────┼───────────────────┐          categories(children)
                    ▼                   ▼                   ▼
        ┌──────────────────┐   ┌──────────────────┐   ┌────────────────────┐
        │ product_variants │   │ product_images   │   │ product_attributes │
        ├──────────────────┤   ├──────────────────┤   ├────────────────────┤
        │ id (PK)          │   │ id (PK)          │   │ id (PK)            │
        │ product_id (FK)  │   │ product_id (FK)  │   │ product_id (FK)    │
        │ sku (UQ)         │   │ variant_id NULL  │   │ name               │
        │ price            │   │ image            │   │ value              │
        │ sale_price       │   │ is_thumbnail     │   └────────────────────┘
        │ stock_quantity   │   │ sort_order       │
        │ color_id (FK)    │   └──────────────────┘
        │ size_id (FK)     │
        │ status           │
        │ created_at       │
        │ updated_at       │
        │ deleted_at       │
        └──────┬───────┬───┘
               │ N:1   │ N:1
               ▼       ▼
        ┌──────────────┐       ┌──────────────┐
        │    colors    │       │    sizes     │
        ├──────────────┤       ├──────────────┤
        │ id (PK)      │       │ id (PK)      │
        │ name (UQ)    │       │ name (UQ)    │
        │ hex_code     │       │ sort_order   │
        │ sort_order   │       └──────────────┘
        └──────────────┘

Checkout / Orders / Payment
┌──────────────┐       1:N       ┌──────────────────┐       1:N       ┌──────────────────┐
│    users     │───────────────►│     orders       │───────────────►│   order_items    │
├──────────────┤                 ├──────────────────┤                 ├──────────────────┤
│ id (PK)      │                 │ id (PK)          │                 │ id (PK)          │
│ email (UQ)   │                 │ user_id (FK)     │                 │ order_id (FK)    │
└──────┬───────┘                 │ order_code (UQ)  │                 │ variant_id NULL  │
       │ 1:1                     │ status           │                 │ product_name     │
       ▼                         │ subtotal         │                 │ variant_name     │
┌──────────────┐       1:N       │ shipping_fee     │                 │ sku              │
│    carts     │───────────────►│ discount_amount  │                 │ image            │
├──────────────┤                 │ final_amount     │                 │ price            │
│ id (PK)      │                 │ receiver_name    │                 │ quantity         │
│ user_id (UQ) │                 │ receiver_phone   │                 │ subtotal         │
│ created_at   │                 │ receiver_address │                 │ status           │
└──────────────┘                 │ payment_method   │                 │ created_at       │
       │ 1:N                     │ payment_status   │                 └────────┬─────────┘
       ▼                         │ created_at       │                          │ N:1 optional
┌──────────────┐                 │ updated_at       │                          ▼
│  cart_items  │                 └──────┬───────────┘                 ┌──────────────────┐
├──────────────┤                        │ 1:N                         │ product_variants │
│ id (PK)      │                        ▼                             ├──────────────────┤
│ cart_id (FK) │                 ┌──────────────┐                     │ id (PK)          │
│ variant_id   │                 │   payments   │                     │ sku (UQ)         │
│ quantity     │                 ├──────────────┤                     └──────────────────┘
└──────┬───────┘                 │ id (PK)      │
       │ N:1                     │ order_id     │
       ▼                         │ provider     │
product_variants                 │ transaction_code │
                                 │ amount       │
                                 │ status       │
                                 │ paid_at      │
                                 │ created_at   │
                                 │ updated_at   │
                                 └──────┬───────┘
                                        │ 1:N
                                        ▼
                                 ┌──────────────────────┐
                                 │ payment_transactions │
                                 ├──────────────────────┤
                                 │ id (PK)              │
                                 │ payment_id (FK)      │
                                 │ transaction_code     │
                                 │ status               │
                                 │ gateway_response     │
                                 │ created_at           │
                                 └──────────────────────┘

┌──────────────────┐       1:N       ┌────────────────┐       N:1       ┌──────────────┐
│     coupons      │◄───────────────│ coupon_usages  │───────────────►│    orders    │
├──────────────────┤                 ├────────────────┤                 ├──────────────┤
│ id (PK)          │                 │ id (PK)        │                 │ id (PK)      │
│ code (UQ)        │                 │ coupon_id (FK) │                 │ order_code   │
│ type             │                 │ user_id (FK)   │                 └──────────────┘
│ value            │                 │ order_id (UQ)  │
│ min_order_amount │                 │ discount_amt   │
│ max_discount     │                 │ used_at        │
│ usage_limit      │                 └───────┬────────┘
│ used_count       │                         │ N:1
│ start_date       │                         ▼
│ end_date         │                  ┌──────────────┐
│ status           │                  │    users     │
└──────────────────┘                  └──────────────┘

Review / Wishlist / Inventory / History
┌──────────────┐       1:N       ┌──────────────┐       N:1       ┌──────────────┐
│    users     │───────────────►│   reviews    │◄───────────────│   products   │
├──────────────┤                 ├──────────────┤                 ├──────────────┤
│ id (PK)      │                 │ id (PK)      │                 │ id (PK)      │
│ email (UQ)   │                 │ user_id      │                 │ slug (UQ)    │
└──────────────┘                 │ product_id   │                 └──────────────┘
                                 │ order_item_id│
                                 │ rating       │ N:1
                                 │ comment      │───────────────►┌──────────────────┐
                                 │ created_at   │                 │   order_items    │
                                 └──────┬───────┘                 ├──────────────────┤
                                        │ 1:N                     │ id (PK)          │
                                        ▼                         │ order_id (FK)    │
                                 ┌──────────────┐                 │ variant_id NULL  │
                                 │ review_images│                 └────────┬─────────┘
                                 ├──────────────┤                          │ N:1
                                 │ id (PK)      │                          ▼
                                 │ review_id    │                   ┌──────────────┐
                                 │ image        │                   │    orders    │
                                 └──────────────┘                   ├──────────────┤
                                                                    │ id (PK)      │
                                                                    │ order_code   │
                                                                    └──────┬───────┘
                                                                           │ 1:N
                                                                           ▼
                                                                  ┌────────────────────────┐
                                                                  │ order_status_histories │
                                                                  ├────────────────────────┤
                                                                  │ id (PK)                │
                                                                  │ order_id (FK)          │
                                                                  │ from_status            │
                                                                  │ to_status              │
                                                                  │ changed_by NULL        │
                                                                  │ reason                 │
                                                                  │ created_at             │
                                                                  └───────────┬────────────┘
                                                                              │ N:1
                                                                              ▼
                                                                       ┌──────────────┐
                                                                       │    users     │
                                                                       └──────────────┘

Order-product relationship is represented by order_items:
orders 1:N order_items, and product_variants 1:N order_items.

┌──────────────┐       N:M       ┌──────────────┐
│    users     │◄───────────────►│   products   │
└──────────────┘  via wishlists  └──────┬───────┘
                                        │ 1:N
                                        ▼
                                ┌──────────────┐
                                │  wishlists   │
                                ├──────────────┤
                                │ id (PK)      │
                                │ user_id      │
                                │ product_id   │
                                │ created_at   │
                                └──────────────┘

┌──────────────────┐       1:N       ┌────────────────┐
│ product_variants │───────────────►│ inventory_logs │
├──────────────────┤                 ├────────────────┤
│ id (PK)          │                 │ id (PK)        │
│ sku (UQ)         │                 │ variant_id     │
│ stock_quantity   │                 │ change_qty     │
└──────────────────┘                 │ type           │
                                     │ reference_type │
                                     │ reference_id   │
                                     │ reason         │
                                     │ created_at     │
                                     └────────────────┘
```

---

## Tables

### users

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| full_name | VARCHAR(150) | NOT NULL | User full name |
| email | VARCHAR(255) | NOT NULL, UNIQUE | Login email |
| password | VARCHAR(255) | NOT NULL | BCrypt hash only |
| birth_date | DATE | NOT NULL | User birth date |
| avatar | VARCHAR(500) | NULLABLE | Avatar image path/URL |
| gender | VARCHAR(20) | NOT NULL | Enum: MALE, FEMALE, OTHER |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |
| updated_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP, updated by trigger | |
| deleted_at | TIMESTAMPTZ | NULLABLE | Soft delete marker |

Indexes:
- `INDEX idx_users_deleted_at ON users(deleted_at)`

Notes:
- `password` must store BCrypt hashes, never raw passwords.
- `email` should be normalized to lowercase before saving.
- Soft-deleted users should not be physically deleted immediately because orders, reviews, payments, and audit history may still reference them.
- Cleanup job: anonymize or hard-delete only after the legal retention window; keep order/payment references readable.

### roles

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| name | VARCHAR(50) | NOT NULL, UNIQUE | ADMIN, MANAGER, STAFF, USER |
| description | VARCHAR(255) | NULLABLE | |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |
| updated_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP, updated by trigger | |

### user_role (Join Table)

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| user_id | BIGINT | FK -> users(id), NOT NULL | |
| role_id | BIGINT | FK -> roles(id), NOT NULL | |

- Composite PK: `(user_id, role_id)`
- ON DELETE CASCADE for both FKs

Indexes:
- `INDEX idx_user_role_role_id ON user_role(role_id)`

### permissions

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| name | VARCHAR(100) | NOT NULL | e.g. CREATE_PRODUCT, VIEW_ORDER |
| api_path | VARCHAR(255) | NOT NULL | e.g. /api/v1/products |
| method | VARCHAR(10) | NOT NULL | GET, POST, PUT, PATCH, DELETE |
| module | VARCHAR(100) | NOT NULL | e.g. USER, PRODUCT, ORDER |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |
| updated_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP, updated by trigger | |

Indexes:
- `INDEX idx_permissions_module ON permissions(module)`

Notes:
- `name` is not unique by itself because the same conceptual permission can exist across versions/modules. The real uniqueness is `api_path + method`.

### permission_role (Join Table)

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| permission_id | BIGINT | FK -> permissions(id), NOT NULL | |
| role_id | BIGINT | FK -> roles(id), NOT NULL | |

- Composite PK: `(permission_id, role_id)`
- ON DELETE CASCADE for both FKs

Indexes:
- `INDEX idx_permission_role_role_id ON permission_role(role_id)`

### refresh_tokens

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| token | VARCHAR(512) | NOT NULL, UNIQUE | Refresh token hash |
| user_id | BIGINT | FK -> users(id), NOT NULL | Token owner |
| expires_at | TIMESTAMPTZ | NOT NULL | Expiration time |
| revoked | BOOLEAN | NOT NULL, DEFAULT false | true = invalidated |
| device_info | VARCHAR(255) | NULLABLE | Parsed user agent/device label |
| ip_address | VARCHAR(45) | NULLABLE | IPv4 or IPv6 |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |

Indexes:
- `INDEX idx_refresh_tokens_user_id ON refresh_tokens(user_id)`
- `INDEX idx_refresh_tokens_expires_at ON refresh_tokens(expires_at)`

Notes:
- Store a SHA-512 hash of the raw refresh JWT, not the raw token.
- Cleanup job can delete expired or revoked rows based on token retention policy.
- Prefer `ON DELETE RESTRICT` for `user_id`; revoke tokens when users are soft-deleted.

### user_addresses

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| user_id | BIGINT | FK -> users(id), NOT NULL | Address owner |
| receiver_name | VARCHAR(150) | NOT NULL | |
| phone | VARCHAR(20) | NOT NULL | |
| province | VARCHAR(100) | NOT NULL | |
| district | VARCHAR(100) | NOT NULL | |
| ward | VARCHAR(100) | NOT NULL | |
| address_detail | VARCHAR(255) | NOT NULL | Street/house detail |
| is_default | BOOLEAN | NOT NULL, DEFAULT false | Default shipping address |

Indexes:
- `INDEX idx_user_addresses_user_id ON user_addresses(user_id)`

Constraints:
- One default address per user: `UNIQUE (user_id) WHERE is_default = true`

Notes:
- Use a partial unique constraint/index to enforce one default address per user.
- Physical delete is acceptable for address records if they are not referenced by orders. Orders store receiver snapshot fields separately.

### brands

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| name | VARCHAR(150) | NOT NULL | Brand name |
| slug | VARCHAR(180) | NOT NULL, UNIQUE | SEO-friendly identifier |
| description | TEXT | NULLABLE | |
| status | VARCHAR(20) | NOT NULL, DEFAULT 'ACTIVE' | ACTIVE, INACTIVE |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |
| updated_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP, updated by trigger | |
| deleted_at | TIMESTAMPTZ | NULLABLE | Soft delete marker |

Indexes:
- `INDEX idx_brands_deleted_at ON brands(deleted_at)`

Notes:
- Cleanup job: hard-delete soft-deleted brands only after no active products reference them.

### categories

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| name | VARCHAR(150) | NOT NULL | Category name |
| slug | VARCHAR(180) | NOT NULL, UNIQUE | SEO-friendly identifier |
| parent_id | BIGINT | FK -> categories(id), NULLABLE | Parent category |
| status | VARCHAR(20) | NOT NULL, DEFAULT 'ACTIVE' | ACTIVE, INACTIVE |
| sort_order | INT | NOT NULL, DEFAULT 0 | Display order |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |
| updated_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP, updated by trigger | |
| deleted_at | TIMESTAMPTZ | NULLABLE | Soft delete marker |

Indexes:
- `INDEX idx_categories_parent_id ON categories(parent_id)`
- `INDEX idx_categories_deleted_at ON categories(deleted_at)`

Notes:
- `parent_id` should use `ON DELETE SET NULL` to avoid deleting child categories accidentally.
- Application logic should prevent circular parent-child category trees.
- Cleanup job: hard-delete soft-deleted categories only after child categories/products are moved or archived.

### colors

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| name | VARCHAR(80) | NOT NULL, UNIQUE | Color display name |
| hex_code | VARCHAR(7) | NULLABLE | e.g. #000000 |
| sort_order | INT | NULLABLE | Display order |

### sizes

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| name | VARCHAR(50) | NOT NULL, UNIQUE | Size display name |
| sort_order | INT | NULLABLE | Display order |

### products

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| name | VARCHAR(255) | NOT NULL | Product name |
| slug | VARCHAR(280) | NOT NULL, UNIQUE | SEO-friendly identifier |
| description | TEXT | NULLABLE | Product detail |
| category_id | BIGINT | FK -> categories(id), NOT NULL | Main category |
| brand_id | BIGINT | FK -> brands(id), NOT NULL | Product brand |
| status | VARCHAR(20) | NOT NULL, DEFAULT 'DRAFT' | DRAFT, ACTIVE, INACTIVE, OUT_OF_STOCK |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |
| updated_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP, updated by trigger | |
| deleted_at | TIMESTAMPTZ | NULLABLE | Soft delete marker |

Indexes:
- `INDEX idx_products_category_id ON products(category_id)`
- `INDEX idx_products_brand_id ON products(brand_id)`
- `INDEX idx_products_deleted_at ON products(deleted_at)`

Notes:
- `DRAFT`: product is being prepared by admin/staff and is not visible on the storefront yet.
- `ACTIVE`: product is visible and can be sold if at least one active variant has stock.
- `INACTIVE`: product is hidden/disabled manually but kept in the catalog.
- `OUT_OF_STOCK`: product is visible or searchable depending on business policy, but cannot be purchased.
- Use `ON DELETE RESTRICT` for `category_id` and `brand_id` to prevent breaking catalog history.
- If a category or brand is no longer used, soft-delete or mark inactive first.
- Cleanup job: hard-delete products only after order retention rules allow it; otherwise keep soft-deleted products for historical reporting.

### product_variants

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| product_id | BIGINT | FK -> products(id), NOT NULL | Parent product |
| sku | VARCHAR(100) | NOT NULL, UNIQUE | Stock keeping unit |
| price | NUMERIC(15,2) | NOT NULL, CHECK price >= 0 | Regular price |
| sale_price | NUMERIC(15,2) | NULLABLE, CHECK sale_price >= 0 | Sale price |
| stock_quantity | INT | NOT NULL, DEFAULT 0, CHECK stock_quantity >= 0 | Current sellable stock |
| color_id | BIGINT | FK -> colors(id), NULLABLE | Variant color |
| size_id | BIGINT | FK -> sizes(id), NULLABLE | Variant size |
| status | VARCHAR(20) | NOT NULL, DEFAULT 'ACTIVE' | ACTIVE, INACTIVE, OUT_OF_STOCK, DISCONTINUED |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |
| updated_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP, updated by trigger | |
| deleted_at | TIMESTAMPTZ | NULLABLE | Soft delete marker |

Indexes:
- `INDEX idx_product_variants_product_id ON product_variants(product_id)`
- `INDEX idx_product_variants_color_id ON product_variants(color_id)`
- `INDEX idx_product_variants_size_id ON product_variants(size_id)`
- `INDEX idx_product_variants_deleted_at ON product_variants(deleted_at)`

Constraints:
- Unique SKU: `UNIQUE (sku)`
- Unique variant option per product: `UNIQUE (product_id, color_id, size_id)`

Notes:
- Add check constraint: `sale_price IS NULL OR sale_price <= price`.
- The composite unique constraint works well when each product has at most one variant per color/size pair.
- If a product can have duplicate color/size variants for special releases, remove `idx_product_variants_product_color_size` and rely on unique SKU only.
- Cleanup job: keep soft-deleted variants while order/reporting history can still reference them; purge only after retention policy allows it.

### product_images

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| product_id | BIGINT | FK -> products(id), NOT NULL | Parent product |
| variant_id | BIGINT | FK -> product_variants(id), NULLABLE | Optional variant-specific image |
| image | VARCHAR(500) | NOT NULL | Image path/URL |
| is_thumbnail | BOOLEAN | NOT NULL, DEFAULT false | Thumbnail flag |
| sort_order | INT | NOT NULL, DEFAULT 0 | Display order |

Indexes:
- `INDEX idx_product_images_product_id ON product_images(product_id)`
- `INDEX idx_product_images_variant_id ON product_images(variant_id)`

Constraints:
- One product thumbnail: `UNIQUE (product_id) WHERE is_thumbnail = true AND variant_id IS NULL`
- One variant thumbnail: `UNIQUE (variant_id) WHERE is_thumbnail = true AND variant_id IS NOT NULL`

Notes:
- Partial unique constraints prevent more than one thumbnail per product or per variant.
- `variant_id` should use `ON DELETE SET NULL` if keeping product-level image history matters, or `ON DELETE CASCADE` if variant images should disappear with the variant.

### product_attributes

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| product_id | BIGINT | FK -> products(id), NOT NULL | Parent product |
| name | VARCHAR(100) | NOT NULL | Attribute name |
| value | VARCHAR(255) | NOT NULL | Attribute value |

Indexes:
- `INDEX idx_product_attributes_product_id ON product_attributes(product_id)`

Constraints:
- Unique attribute name per product: `UNIQUE (product_id, name)`

### carts

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| user_id | BIGINT | FK -> users(id), NOT NULL, UNIQUE | Cart owner |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |

Notes:
- One active cart per user is enough for the current scope.

### cart_items

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| cart_id | BIGINT | FK -> carts(id), NOT NULL | Parent cart |
| variant_id | BIGINT | FK -> product_variants(id), NOT NULL | Variant in cart |
| quantity | INT | NOT NULL, CHECK quantity > 0 | Item quantity |

Indexes:
- `INDEX idx_cart_items_variant_id ON cart_items(variant_id)`

Constraints:
- Unique cart item variant: `UNIQUE (cart_id, variant_id)`

Notes:
- `cart_id` should use `ON DELETE CASCADE`.
- `variant_id` should use `ON DELETE RESTRICT`; if a variant is unavailable, mark it inactive/stock 0 rather than deleting it.

### coupons

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| code | VARCHAR(50) | NOT NULL, UNIQUE | Coupon code |
| type | VARCHAR(20) | NOT NULL | PERCENTAGE, FIXED_AMOUNT |
| value | NUMERIC(15,2) | NOT NULL, CHECK value >= 0 | Discount value |
| min_order_amount | NUMERIC(15,2) | NOT NULL, DEFAULT 0, CHECK min_order_amount >= 0 | Minimum order value |
| max_discount | NUMERIC(15,2) | NULLABLE, CHECK max_discount >= 0 | Cap for percentage discounts |
| usage_limit | INT | NULLABLE, CHECK usage_limit >= 0 | Total usage limit |
| used_count | INT | NOT NULL, DEFAULT 0, CHECK used_count >= 0 | Current usage count |
| start_date | TIMESTAMPTZ | NOT NULL | Valid from |
| end_date | TIMESTAMPTZ | NOT NULL | Valid until |
| status | VARCHAR(20) | NOT NULL, DEFAULT 'ACTIVE' | ACTIVE, INACTIVE, EXPIRED |

Indexes:
- `INDEX idx_coupons_valid_range ON coupons(start_date, end_date)`

Notes:
- Add check constraint: `end_date > start_date`.
- `used_count` must be updated transactionally when an order successfully uses a coupon.

### orders

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| user_id | BIGINT | FK -> users(id), NOT NULL | Buyer |
| order_code | VARCHAR(50) | NOT NULL, UNIQUE | Human-readable order code |
| status | VARCHAR(30) | NOT NULL | PENDING, CONFIRMED, SHIPPING, COMPLETED, CANCELLED, REFUNDED |
| subtotal | NUMERIC(15,2) | NOT NULL, CHECK subtotal >= 0 | Sum of item subtotals |
| shipping_fee | NUMERIC(15,2) | NOT NULL, DEFAULT 0, CHECK shipping_fee >= 0 | Shipping fee |
| discount_amount | NUMERIC(15,2) | NOT NULL, DEFAULT 0, CHECK discount_amount >= 0 | Discount applied |
| final_amount | NUMERIC(15,2) | NOT NULL, CHECK final_amount >= 0 | Payable amount |
| receiver_name | VARCHAR(150) | NOT NULL | Snapshot at checkout |
| receiver_phone | VARCHAR(20) | NOT NULL | Snapshot at checkout |
| receiver_address | VARCHAR(500) | NOT NULL | Snapshot at checkout |
| payment_method | VARCHAR(30) | NOT NULL | COD, VNPAY, MOMO, BANK_TRANSFER |
| payment_status | VARCHAR(30) | NOT NULL | UNPAID, PAID, FAILED, REFUNDED |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |
| updated_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP, updated by trigger | |

Indexes:
- `INDEX idx_orders_user_id ON orders(user_id)`

Notes:
- Order receiver fields are snapshots and should not be normalized to `user_addresses`.
- `updated_at` tracks direct order field changes; `order_status_histories` still records every status transition.
- Add check constraint: `final_amount = subtotal + shipping_fee - discount_amount` if totals are always database-enforced. Otherwise keep this validation in service logic to allow rare adjustment cases.

### order_items

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| order_id | BIGINT | FK -> orders(id), NOT NULL | Parent order |
| variant_id | BIGINT | FK -> product_variants(id), NULLABLE | Variant reference, nullable for historical safety |
| product_name | VARCHAR(255) | NOT NULL | Snapshot product name |
| variant_name | VARCHAR(255) | NULLABLE | Snapshot variant label |
| sku | VARCHAR(100) | NOT NULL | Snapshot SKU |
| image | VARCHAR(500) | NULLABLE | Snapshot image |
| price | NUMERIC(15,2) | NOT NULL, CHECK price >= 0 | Snapshot unit price |
| quantity | INT | NOT NULL, CHECK quantity > 0 | |
| subtotal | NUMERIC(15,2) | NOT NULL, CHECK subtotal >= 0 | price * quantity |
| status | VARCHAR(30) | NOT NULL | PENDING, CONFIRMED, CANCELLED, RETURNED |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |

Indexes:
- `INDEX idx_order_items_order_id ON order_items(order_id)`
- `INDEX idx_order_items_variant_id ON order_items(variant_id)`
- `INDEX idx_order_items_status ON order_items(status)`

Notes:
- `order_id` uses `ON DELETE RESTRICT` because order items are commercial history and should not be cascade-deleted with an order.
- `variant_id` is nullable so old order items still remain valid if a variant is removed.
- Snapshot fields (`product_name`, `variant_name`, `sku`, `image`, `price`) must be filled at checkout and never recomputed from catalog tables.
- Add check constraint: `subtotal = price * quantity` if subtotal is always strictly derived.

### payments

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| order_id | BIGINT | FK -> orders(id), NOT NULL | Related order |
| provider | VARCHAR(50) | NOT NULL | COD, VNPAY, MOMO, STRIPE |
| transaction_code | VARCHAR(100) | NULLABLE, UNIQUE | Latest provider transaction code |
| amount | NUMERIC(15,2) | NOT NULL, CHECK amount >= 0 | Payment amount |
| status | VARCHAR(30) | NOT NULL | PENDING, SUCCESS, FAILED, CANCELLED, REFUNDED |
| paid_at | TIMESTAMPTZ | NULLABLE | Payment success time |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |
| updated_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP, updated by trigger | |

Indexes:
- `INDEX idx_payments_order_id ON payments(order_id)`

Notes:
- If only one payment attempt per order is allowed, add `UNIQUE (order_id)`.
- Current design allows multiple payment attempts per order and stores detailed attempts in `payment_transactions`.

### payment_transactions

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| payment_id | BIGINT | FK -> payments(id), NOT NULL | Parent payment |
| transaction_code | VARCHAR(100) | NULLABLE, UNIQUE when not null | Provider transaction ID/code |
| status | VARCHAR(30) | NOT NULL | PENDING, SUCCESS, FAILED, CANCELLED |
| gateway_response | JSONB | NULLABLE | Raw/sanitized provider response |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |

Indexes:
- `INDEX idx_payment_transactions_payment_id ON payment_transactions(payment_id)`

Notes:
- `payment_id` uses `ON DELETE RESTRICT` because payment transaction records are financial/audit history.
- Do not store sensitive payment data such as card number, CVV, raw access tokens, or provider secrets.

### coupon_usages

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| coupon_id | BIGINT | FK -> coupons(id), NOT NULL | Used coupon |
| user_id | BIGINT | FK -> users(id), NOT NULL | User who used the coupon |
| order_id | BIGINT | FK -> orders(id), NOT NULL, UNIQUE | Order where coupon was used |
| discount_amount | NUMERIC(15,2) | NOT NULL, CHECK discount_amount >= 0 | Actual discount |
| used_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | Usage time |

Indexes:
- `INDEX idx_coupon_usages_coupon_id ON coupon_usages(coupon_id)`
- `INDEX idx_coupon_usages_user_id ON coupon_usages(user_id)`
- `INDEX idx_coupon_usages_used_at ON coupon_usages(used_at)`

Notes:
- `UNIQUE(order_id)` prevents applying multiple coupons to one order.
- If one user can use a coupon only once, add `UNIQUE (coupon_id, user_id)`.

### reviews

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| user_id | BIGINT | FK -> users(id), NOT NULL | Reviewer |
| product_id | BIGINT | FK -> products(id), NOT NULL | Reviewed product |
| order_item_id | BIGINT | FK -> order_items(id), NOT NULL | Purchased order item being reviewed |
| rating | SMALLINT | NOT NULL, CHECK rating BETWEEN 1 AND 5 | Star rating |
| comment | TEXT | NULLABLE | Review content |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |

Indexes:
- `INDEX idx_reviews_product_id ON reviews(product_id)`
- `INDEX idx_reviews_user_id ON reviews(user_id)`
- `INDEX idx_reviews_order_item_id ON reviews(order_item_id)`

Constraints:
- Unique review per purchased order item: `UNIQUE (user_id, order_item_id)`

Notes:
- Review eligibility should be validated against the order item: the order must belong to the reviewing user, must be completed, and the order item must correspond to the reviewed product/variant.
- `product_id` is intentionally kept for fast product review listing, while `order_item_id` proves the exact purchased item.

### review_images

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| review_id | BIGINT | FK -> reviews(id), NOT NULL | Parent review |
| image | VARCHAR(500) | NOT NULL | Image path/URL |

Indexes:
- `INDEX idx_review_images_review_id ON review_images(review_id)`

Notes:
- `review_id` should use `ON DELETE CASCADE`.

### inventory_logs

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| variant_id | BIGINT | FK -> product_variants(id), NOT NULL | Affected variant |
| change_quantity | INT | NOT NULL | Positive or negative stock change |
| type | VARCHAR(30) | NOT NULL | IMPORT, EXPORT, ORDER, CANCEL, RETURN, ADJUSTMENT |
| reference_type | VARCHAR(50) | NULLABLE | Source object type, e.g. ORDER, ORDER_ITEM, RETURN, MANUAL_ADJUSTMENT |
| reference_id | BIGINT | NULLABLE | Source object id matching `reference_type` |
| reason | VARCHAR(255) | NULLABLE | Human-readable reason |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |

Indexes:
- `INDEX idx_inventory_logs_variant_id ON inventory_logs(variant_id)`
- `INDEX idx_inventory_logs_reference ON inventory_logs(reference_type, reference_id)`

Notes:
- Inventory logs should usually be append-only. Avoid updating/deleting logs except for data retention policies.
- `reference_type` and `reference_id` are nullable because some inventory changes can be manual or imported without a business record at first.
- If one reference field is present, both must be present. Enforce this with `ck_inventory_logs_reference_pair`.
- Do not add polymorphic foreign keys for `reference_id`; validate allowed reference types in service logic.

### wishlists

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| user_id | BIGINT | FK -> users(id), NOT NULL | Wishlist owner |
| product_id | BIGINT | FK -> products(id), NOT NULL | Wished product |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |

Indexes:
- `INDEX idx_wishlists_product_id ON wishlists(product_id)`

Constraints:
- Unique wishlist item: `UNIQUE (user_id, product_id)`

Notes:
- `user_id` should use `ON DELETE CASCADE` only if the user is physically deleted by retention job.
- For normal user deletion, use `users.deleted_at` and keep wishlist cleanup as an async job if needed.

### order_status_histories

| Column | Type | Constraints | Description |
|--------|------|-------------|-------------|
| id | BIGINT GENERATED BY DEFAULT AS IDENTITY | PK | |
| order_id | BIGINT | FK -> orders(id), NOT NULL | Parent order |
| from_status | VARCHAR(30) | NULLABLE | Previous status |
| to_status | VARCHAR(30) | NOT NULL | New status |
| changed_by | BIGINT | FK -> users(id), NULLABLE | Admin/staff user who changed status |
| reason | VARCHAR(255) | NULLABLE | Reason/comment |
| created_at | TIMESTAMPTZ | NOT NULL, DEFAULT CURRENT_TIMESTAMP | |

Indexes:
- `INDEX idx_order_status_histories_order_id ON order_status_histories(order_id)`
- `INDEX idx_order_status_histories_changed_by ON order_status_histories(changed_by)`
- `INDEX idx_order_status_histories_created_at ON order_status_histories(created_at)`

Notes:
- `order_id` uses `ON DELETE RESTRICT` because status history is part of the order audit trail.
- `changed_by` is nullable for system-generated changes.
- Keep histories append-only.

---

## Index Summary

| Table | Index | Fields | Purpose |
|-------|-------|--------|---------|
| users | `idx_users_deleted_at` | `deleted_at` | Soft-delete cleanup/anonymization job |
| user_role | `idx_user_role_role_id` | `role_id` | Fast role-to-user lookup |
| permissions | `idx_permissions_module` | `module` | Filter permissions by module |
| permission_role | `idx_permission_role_role_id` | `role_id` | Fast role-to-permission lookup |
| refresh_tokens | `idx_refresh_tokens_user_id` | `user_id` | Lookup tokens by user |
| refresh_tokens | `idx_refresh_tokens_expires_at` | `expires_at` | Expired token cleanup job |
| user_addresses | `idx_user_addresses_user_id` | `user_id` | Lookup addresses by user |
| brands | `idx_brands_deleted_at` | `deleted_at` | Soft-delete cleanup job |
| categories | `idx_categories_parent_id` | `parent_id` | Load category tree |
| categories | `idx_categories_deleted_at` | `deleted_at` | Soft-delete cleanup job |
| products | `idx_products_category_id` | `category_id` | Filter products by category |
| products | `idx_products_brand_id` | `brand_id` | Filter products by brand |
| products | `idx_products_deleted_at` | `deleted_at` | Soft-delete cleanup/report retention job |
| product_variants | `idx_product_variants_product_id` | `product_id` | Lookup variants by product |
| product_variants | `idx_product_variants_color_id` | `color_id` | Filter variants by color |
| product_variants | `idx_product_variants_size_id` | `size_id` | Filter variants by size |
| product_variants | `idx_product_variants_deleted_at` | `deleted_at` | Soft-delete cleanup/report retention job |
| product_images | `idx_product_images_product_id` | `product_id` | Lookup images by product |
| product_images | `idx_product_images_variant_id` | `variant_id` | Lookup images by variant |
| product_attributes | `idx_product_attributes_product_id` | `product_id` | Lookup attributes by product |
| cart_items | `idx_cart_items_variant_id` | `variant_id` | Lookup cart rows by variant |
| coupons | `idx_coupons_valid_range` | `start_date`, `end_date` | Validate coupon date window |
| orders | `idx_orders_user_id` | `user_id` | Lookup orders by user |
| order_items | `idx_order_items_order_id` | `order_id` | Lookup items by order |
| order_items | `idx_order_items_variant_id` | `variant_id` | Product/variant sales history |
| order_items | `idx_order_items_status` | `status` | Filter item-level state |
| payments | `idx_payments_order_id` | `order_id` | Lookup payments by order |
| payment_transactions | `idx_payment_transactions_payment_id` | `payment_id` | Lookup transactions by payment |
| coupon_usages | `idx_coupon_usages_coupon_id` | `coupon_id` | Lookup usage by coupon |
| coupon_usages | `idx_coupon_usages_user_id` | `user_id` | Lookup usage by user |
| coupon_usages | `idx_coupon_usages_used_at` | `used_at` | Coupon usage reports |
| reviews | `idx_reviews_product_id` | `product_id` | Product review listing |
| reviews | `idx_reviews_user_id` | `user_id` | User review listing |
| reviews | `idx_reviews_order_item_id` | `order_item_id` | Verify review source order item |
| review_images | `idx_review_images_review_id` | `review_id` | Lookup images by review |
| inventory_logs | `idx_inventory_logs_variant_id` | `variant_id` | Variant stock history |
| inventory_logs | `idx_inventory_logs_reference` | `reference_type`, `reference_id` | Trace inventory change source |
| wishlists | `idx_wishlists_product_id` | `product_id` | Product popularity reports |
| order_status_histories | `idx_order_status_histories_order_id` | `order_id` | Load order status timeline |
| order_status_histories | `idx_order_status_histories_changed_by` | `changed_by` | Audit staff/system changes |
| order_status_histories | `idx_order_status_histories_created_at` | `created_at` | Status history reports |

Notes:
- PostgreSQL automatically creates backing indexes for primary key and unique constraints; those are not repeated in this Index Summary.
- Unique business rules are documented under each table's `Constraints` section or column constraints.

Soft-delete cleanup indexes:
- `idx_users_deleted_at ON users(deleted_at)`
- `idx_brands_deleted_at ON brands(deleted_at)`
- `idx_categories_deleted_at ON categories(deleted_at)`
- `idx_products_deleted_at ON products(deleted_at)`
- `idx_product_variants_deleted_at ON product_variants(deleted_at)`

---

## Relationships Summary

| Relationship | Type | Owner Side | Join Config |
|-------------|------|-----------|-------------|
| User -> UserRole -> Role | OneToMany + ManyToOne | UserRole | `@JoinColumn(name = "user_id")`, `@JoinColumn(name = "role_id")` |
| Role -> PermissionRole -> Permission | OneToMany + ManyToOne | PermissionRole | `@JoinColumn(name = "role_id")`, `@JoinColumn(name = "permission_id")` |
| User -> RefreshToken | OneToMany | RefreshToken | `@JoinColumn(name = "user_id")` |
| User -> UserAddress | OneToMany | UserAddress | `@JoinColumn(name = "user_id")` |
| Category -> Category | ManyToOne self-reference | Category | `@JoinColumn(name = "parent_id")` |
| Category -> Product | OneToMany | Product | `@JoinColumn(name = "category_id")` |
| Brand -> Product | OneToMany | Product | `@JoinColumn(name = "brand_id")` |
| Product -> ProductVariant | OneToMany | ProductVariant | `@JoinColumn(name = "product_id")` |
| Product -> ProductImage | OneToMany | ProductImage | `@JoinColumn(name = "product_id")` |
| ProductVariant -> ProductImage | OneToMany optional | ProductImage | `@JoinColumn(name = "variant_id")` |
| ProductVariant -> CartItem | OneToMany | CartItem | `@JoinColumn(name = "variant_id")` |
| User -> Cart | OneToOne | Cart | `@JoinColumn(name = "user_id")` |
| Cart -> CartItem | OneToMany | CartItem | `@JoinColumn(name = "cart_id")` |
| User -> Order | OneToMany | Order | `@JoinColumn(name = "user_id")` |
| Order -> OrderItem | OneToMany | OrderItem | `@JoinColumn(name = "order_id")` |
| Order -> Payment | OneToMany | Payment | `@JoinColumn(name = "order_id")` |
| Payment -> PaymentTransaction | OneToMany | PaymentTransaction | `@JoinColumn(name = "payment_id")` |
| Coupon -> CouponUsage | OneToMany | CouponUsage | `@JoinColumn(name = "coupon_id")` |
| Product -> Review | OneToMany | Review | `@JoinColumn(name = "product_id")` |
| OrderItem -> Review | OneToMany | Review | `@JoinColumn(name = "order_item_id")` |
| Review -> ReviewImage | OneToMany | ReviewImage | `@JoinColumn(name = "review_id")` |
| ProductVariant -> InventoryLog | OneToMany | InventoryLog | `@JoinColumn(name = "variant_id")` |
| User -> Wishlist | OneToMany | Wishlist | `@JoinColumn(name = "user_id")` |
| Order -> OrderStatusHistory | OneToMany | OrderStatusHistory | `@JoinColumn(name = "order_id")` |

### JPA Mapping Notes

- All `@ManyToOne`: use `FetchType.LAZY`.
- All collection relationships: use `FetchType.LAZY`.
- Do not use JPA `@ManyToMany`. Model join tables as explicit entities (`UserRole`, `PermissionRole`) with `@ManyToOne` and `@OneToMany`.
- Avoid returning entities directly from controllers; map to DTOs in service layer.
- Use `@Enumerated(EnumType.STRING)` for statuses, gender, payment method, provider, and log type.
- Always prioritize using Lombok `@Getter`, `@Setter`, `@RequiredArgsConstructor`, `@NoArgsConstructor`, `@Builder`, etc. (only import what is actually used to avoid redundant imports/code).
- Do not use Lombok `@Data` on entities.
- For soft-delete tables, repository queries should filter `deletedAt IS NULL` by default.
- Join tables `user_role` and `permission_role` use composite primary keys and `ON DELETE CASCADE` for both foreign keys.
- Deleting a role physically can cascade join table rows, but production workflows should usually disable/deactivate roles instead.
- Deleting a product should normally be soft delete. Orders and order items must remain readable.
- `updated_at` should be handled by Hibernate `@UpdateTimestamp` and/or PostgreSQL trigger. Keep the strategy consistent.

---

## JPA Entity Mapping Reference

### User.java

```java
@OneToMany(mappedBy = "user", fetch = FetchType.LAZY)
private Set<UserRole> userRoles = new HashSet<>();

@OneToMany(mappedBy = "user", fetch = FetchType.LAZY)
private List<RefreshToken> refreshTokens = new ArrayList<>();

@OneToMany(mappedBy = "user", fetch = FetchType.LAZY)
private List<UserAddress> addresses = new ArrayList<>();
```

### Role.java

```java
@OneToMany(mappedBy = "role", fetch = FetchType.LAZY)
private Set<UserRole> userRoles = new HashSet<>();

@OneToMany(mappedBy = "role", fetch = FetchType.LAZY)
private Set<PermissionRole> permissionRoles = new HashSet<>();
```

### Permission.java

```java
@OneToMany(mappedBy = "permission", fetch = FetchType.LAZY)
private Set<PermissionRole> permissionRoles = new HashSet<>();
```

### UserRole.java

```java
@EmbeddedId
private UserRoleId id;

@MapsId("userId")
@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "user_id", nullable = false)
private User user;

@MapsId("roleId")
@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "role_id", nullable = false)
private Role role;
```

### PermissionRole.java

```java
@EmbeddedId
private PermissionRoleId id;

@MapsId("permissionId")
@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "permission_id", nullable = false)
private Permission permission;

@MapsId("roleId")
@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "role_id", nullable = false)
private Role role;
```

### Product.java

```java
@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "category_id", nullable = false)
private Category category;

@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "brand_id", nullable = false)
private Brand brand;

@OneToMany(mappedBy = "product", fetch = FetchType.LAZY)
private List<ProductVariant> variants = new ArrayList<>();

@OneToMany(mappedBy = "product", fetch = FetchType.LAZY)
private List<ProductImage> images = new ArrayList<>();
```

### ProductVariant.java

```java
@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "product_id", nullable = false)
private Product product;

@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "color_id")
private Color color;

@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "size_id")
private Size size;
```

### Order.java

```java
@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "user_id", nullable = false)
private User user;

@OneToMany(mappedBy = "order", fetch = FetchType.LAZY)
private List<OrderItem> items = new ArrayList<>();

@OneToMany(mappedBy = "order", fetch = FetchType.LAZY)
private List<Payment> payments = new ArrayList<>();
```

### Payment.java

```java
@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "order_id", nullable = false)
private Order order;

@OneToMany(mappedBy = "payment", fetch = FetchType.LAZY)
private List<PaymentTransaction> transactions = new ArrayList<>();
```

### Review.java

```java
@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "user_id", nullable = false)
private User user;

@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "product_id", nullable = false)
private Product product;

@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "order_item_id", nullable = false)
private OrderItem orderItem;

@OneToMany(mappedBy = "review", fetch = FetchType.LAZY)
private List<ReviewImage> images = new ArrayList<>();
```

---

## Sample Data

### Roles

| id | name | description |
|----|------|-------------|
| 1 | ADMIN | Full system administration |
| 2 | MANAGER | Product, order, coupon, and report management |
| 3 | STAFF | Daily operation support |
| 4 | USER | Customer account |

### Permissions (examples)

| id | name | api_path | method | module |
|----|------|----------|--------|--------|
| 1 | CREATE_PRODUCT | /api/v1/products | POST | PRODUCT |
| 2 | UPDATE_PRODUCT | /api/v1/products/{id} | PUT | PRODUCT |
| 3 | DELETE_PRODUCT | /api/v1/products/{id} | DELETE | PRODUCT |
| 4 | VIEW_PRODUCTS | /api/v1/products | GET | PRODUCT |
| 5 | CREATE_ORDER | /api/v1/orders | POST | ORDER |
| 6 | UPDATE_ORDER_STATUS | /api/v1/orders/{id}/status | PATCH | ORDER |
| 7 | VIEW_ORDERS | /api/v1/orders | GET | ORDER |
| 8 | CREATE_COUPON | /api/v1/coupons | POST | COUPON |
| 9 | UPDATE_COUPON | /api/v1/coupons/{id} | PUT | COUPON |
| 10 | VIEW_USERS | /api/v1/users | GET | USER |
| 11 | ASSIGN_ROLE | /api/v1/users/{id}/roles | PUT | RBAC |
| 12 | VIEW_PERMISSIONS | /api/v1/permissions | GET | RBAC |

### Role -> Permission Mapping

| Role | Permissions |
|------|-------------|
| ADMIN | All permissions |
| MANAGER | Product, order, coupon, inventory, review moderation |
| STAFF | View/update orders, view products, update inventory, support customers |
| USER | Own profile, cart, checkout, orders, reviews, wishlist |

### Categories

| id | name | slug | parent_id | status | sort_order |
|----|------|------|-----------|--------|------------|
| 1 | Men | men | NULL | ACTIVE | 1 |
| 2 | Women | women | NULL | ACTIVE | 2 |
| 3 | T-Shirts | t-shirts | 1 | ACTIVE | 1 |
| 4 | Dresses | dresses | 2 | ACTIVE | 1 |

### Brands

| id | name | slug | status |
|----|------|------|--------|
| 1 | VelaWear | velawear | ACTIVE |
| 2 | Urban Thread | urban-thread | ACTIVE |

### Colors

| id | name | hex_code | sort_order |
|----|------|----------|------------|
| 1 | Black | #000000 | 1 |
| 2 | White | #FFFFFF | 2 |
| 3 | Navy | #1F3A5F | 3 |

### Sizes

| id | name | sort_order |
|----|------|------------|
| 1 | S | 1 |
| 2 | M | 2 |
| 3 | L | 3 |
| 4 | XL | 4 |

### Coupon

| id | code | type | value | min_order_amount | max_discount | usage_limit | status |
|----|------|------|-------|------------------|--------------|-------------|--------|
| 1 | WELCOME10 | PERCENTAGE | 10.00 | 300000.00 | 100000.00 | 1000 | ACTIVE |
| 2 | FREESHIP50 | FIXED_AMOUNT | 50000.00 | 500000.00 | NULL | 500 | ACTIVE |

---

## Migration Notes

- **Total tables: 28** — users, roles, user_role, permissions, permission_role, refresh_tokens, user_addresses, brands, categories, colors, sizes, products, product_variants, product_images, product_attributes, carts, cart_items, coupons, orders, order_items, payments, payment_transactions, coupon_usages, reviews, review_images, inventory_logs, wishlists, order_status_histories.
- PostgreSQL is the only supported database for dev, test, and prod profiles.
- Schema should be managed by Flyway migrations:
  - `dev` profile: Flyway runs migrations; Hibernate should use `validate` or controlled `update` only while prototyping.
  - `test` profile: PostgreSQL test database; Flyway runs migrations; Hibernate validates.
  - `prod` profile: Flyway migrations only; Hibernate validates.
- Use `TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP` for audit timestamps.
- For `updated_at`, create a reusable PostgreSQL trigger function:

```sql
CREATE OR REPLACE FUNCTION set_updated_at()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = CURRENT_TIMESTAMP;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```

- Add `BEFORE UPDATE` triggers for tables with `updated_at`: users, roles, permissions, brands, categories, products, product_variants, orders, payments.
- Soft-delete cleanup jobs must index and purge based on business importance:
  - Low importance: wishlists can be purged sooner.
  - Medium importance: carts, refresh tokens can be cleaned regularly.
  - High importance: users, products, orders, payments should be retained longer or anonymized instead of hard-deleted.
- Current indexed soft-delete cleanup targets:
  - `users.deleted_at`: anonymize or hard-delete only after retention window.
  - `brands.deleted_at`: hard-delete only after no active products reference the brand.
  - `categories.deleted_at`: hard-delete only after children/products are moved or archived.
  - `products.deleted_at`: retain for order/reporting history, then purge after retention policy.
  - `product_variants.deleted_at`: retain while order/reporting history can reference the variant, then purge after retention policy.
- Coupon usage must be applied inside one database transaction:
  - Lock the coupon row with `SELECT ... FOR UPDATE` or equivalent JPA pessimistic lock.
  - Re-check status, valid date range, `usage_limit`, and `used_count`.
  - Insert `coupon_usages`.
  - Increment `coupons.used_count`.
  - Commit together with order creation/payment flow according to checkout design.
- Join table migration must use composite primary keys:
  - `PRIMARY KEY (user_id, role_id)` for `user_role`
  - `PRIMARY KEY (permission_id, role_id)` for `permission_role`
- Join table foreign keys should use `ON DELETE CASCADE` for both sides.
- Order, payment, review, and inventory records should not be hard-deleted during normal application workflows.
- Business history child records such as `order_items`, `payment_transactions`, and `order_status_histories` should use `ON DELETE RESTRICT` to prevent accidental cascade deletion of audit/commercial history.
- Use `JSONB` only for external gateway response data where the shape can vary. Core business fields should remain relational columns.

---

## Design Review Notes

- The current database design has resolved the earlier open notes for orders, order item snapshots, coupon concurrency, and variant soft delete.
- `colors` and `sizes` are intentionally simple master data without audit timestamps for now.
- Persistent `notifications` table is intentionally omitted. Important order/payment events should be sent by email, while short-lived UI messages can be handled outside database state.
- Remaining business decision: decide exact retention windows for users, products, and variants before writing cleanup jobs.
